---
description: 입문자 리마인드 — 체크리스트로 재-앵커하고 지정 파일을 감사·보강
argument-hint: <감사할 파일 경로>
---

먼저 `docs/beginner-reminder.md` 를 처음부터 다시 읽어라(리마인드 = 재-앵커).

그다음 아래 파일(들)을 그 3대 체크로 감사하라 — 🧠 오해예측 · ⏱ 시간 시뮬레이션(ExprReduce/MemoryModel/StackViz 재사용) · 🔁 예측→반복 드릴.

대상: $ARGUMENTS

"정적 표 + 예제 1개"로 끝난 갭을 찾아, 고치기 전 **갭 목록을 짧게 보고**한 뒤 → 시뮬레이션+반복으로 보강 → 실브라우저 스크린샷 검증 → 커밋한다.
