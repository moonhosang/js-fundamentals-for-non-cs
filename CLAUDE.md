# JS입문 — 프로젝트 지침

## 🔔 큐: `초보감사` (입문자 인지 감사 스위치)
사용자가 **`초보감사`**(별칭: `초보 리마인드` · `인지감사` · `CAC`)라고 하면 — 다른 무엇보다 **먼저**, 즉시:

1. **[docs/beginner-reminder.md](docs/beginner-reminder.md)(코어 스위치) + [docs/cognitive-audit-canvas.md](docs/cognitive-audit-canvas.md)(함정 레지스트리) 를 실제로 다시 읽는다.** (리마인드=재-앵커. 기억·각오 말고 파일을 읽을 것.)
2. 지시된 파일(형식 `초보감사 <파일|강의id>`, 없으면 현재/직전 대상)을 **코어 6 + 관련 함정(B)** 으로 감사한다.
3. "정적 표 + 예제 1개"로 끝난 갭을 **짧게 보고 → 시뮬레이션+반복으로 보강 → 실브라우저 검증 → 커밋.**

이 큐의 이유: 내 기본값(매끄러운 "전문가-완결")이 매 턴 표류하므로, 사용자가 **한 문장**으로 다시 규율로 끌어당긴다. 큐는 **하드 인터럽트**로 취급한다(소프트 제안 아님). 문구/함정을 바꾸면 위 두 문서만 고친다(SSOT).
